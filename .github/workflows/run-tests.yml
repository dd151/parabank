name: Java CI with Maven

on:
  workflow_dispatch:
    inputs:
      cucumberTag:
        description: 'Cucumber tag to run'
        required: true
        default: '@Registration'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven

    - name: Run Maven Tests
      run: mvn clean test -Dcucumber.filter.tags="${{ github.event.inputs.cucumberTag }}" -Dheadless

    - name: Generate HTML report
      run: mvn surefire-report:report

    - name: Upload Surefire XML Reports
      uses: actions/upload-artifact@v4
      with:
        name: surefire-xml
        path: target/surefire-reports/

    - name: Upload Surefire HTML Report
      uses: actions/upload-artifact@v4
      with:
        name: surefire-html
        path: target/site/surefire-report/